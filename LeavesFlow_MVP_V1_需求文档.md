# LeavesFlow MVP V1 需求文档

## 1. 产品目标

LeavesFlow V1 的目标是落地一个最小可用的 AI 任务导航闭环：

```text
标签选择 -> 15字内目标输入 -> AI 拆解任务 -> 推荐路径 / 工具 / 资源 -> 用户打卡完成 -> AI 提炼能力标签 -> 回写用户画像
```

V1 不追求完整社区、自动 Agent 执行、官方认证体系或复杂成长树动画，而是优先验证 LeavesFlow 最核心的业务假设：

- 用户不需要会写 Prompt，也能获得个性化 AI 任务拆解。
- AI 能根据用户标签与短目标生成可执行路径。
- 每个任务节点都能给出推荐工具、资源链接和完成标准。
- 用户完成任务后，系统能将成果转化为新的技能标签。
- 用户画像会随着任务完成持续成长，形成业务闭环。

---

## 2. 核心业务闭环

V1 的完整流程如下：

```text
用户选择标签
    ↓
输入 15 字以内任务目标
    ↓
系统生成隐藏 Prompt
    ↓
调用大模型 / MCP / Skill
    ↓
生成任务拆解结果
    ↓
展示任务节点、执行路径、工具和资源
    ↓
用户逐个打卡完成
    ↓
大模型分析已完成任务
    ↓
生成新的技能标签
    ↓
回写到用户标签体系
```

核心闭环可以总结为：

```text
一次任务完成，必须带来一次用户画像成长。
```

---

## 3. 核心功能一：标签选择与 Prompt 封装

### 3.1 功能目标

通过预设标签封装 Prompt，让用户不需要学习提示词工程，也能获得适合自身背景的 AI 输出。

用户只需要选择符合自己的标签，系统在后台自动拼接上下文、控制任务拆解难度、推荐合适工具与资源。

### 3.2 标签类型

#### 身份标签

- 大学生
- 职场新人
- 转行学习者
- 创业团队
- 零基础用户

#### 专业背景

- 计算机
- 设计
- 商科
- 文科
- 工科
- 其他

#### 能力阶段

- 零基础
- 入门
- 有基础
- 有项目经验

#### 目标类型

- 做项目
- 学技能
- 准备比赛
- 求职面试
- 写文档
- 做作品集

#### 时间周期

- 1 天内
- 3 天内
- 1 周内
- 2-4 周
- 1-3 个月

#### 输出偏好

- 步骤详细
- 快速上手
- 工具优先
- 学习优先
- 项目交付优先

### 3.3 系统行为

系统根据用户标签生成隐藏上下文，例如：

```text
用户是大学生，计算机相关背景，当前能力为入门，目标是做项目，希望在 2-4 周内完成，输出偏好是项目交付优先。
请将用户输入的短目标拆解为适合其阶段的可执行任务路径。
```

标签需要影响以下输出结果：

- 任务难度
- 拆解颗粒度
- 推荐工具类型
- 推荐资源深度
- 输出语气
- 完成标准
- 技能标签生成方向

---

## 4. 核心功能二：15 字以内任务目标输入

### 4.1 功能目标

用户只需要输入一个极短目标，就可以启动 AI 任务拆解流程。

示例：

```text
做一个AI网站
准备互联网面试
写商业计划书
学习AI产品设计
完成React项目
```

### 4.2 输入限制

- 中文目标建议限制在 15 字以内。
- 英文目标建议限制在 30 个字符以内。
- 不要求用户描述详细背景。
- 不要求用户编写 Prompt。
- 可以提供示例目标按钮帮助用户快速开始。

### 4.3 结构化请求示例

系统将用户标签和短目标合并成结构化请求：

```json
{
  "userProfile": {
    "identity": "大学生",
    "major": "计算机",
    "level": "入门",
    "goalType": "做项目",
    "timeRange": "2-4周",
    "outputPreference": "项目交付优先"
  },
  "goal": "做一个AI网站"
}
```

---

## 5. 核心功能三：大模型任务拆解

### 5.1 功能目标

系统接入大模型，通过 MCP 工具或 Skill 对用户目标进行拆解，并生成可执行任务路径。

### 5.2 模型输出内容

每次生成结果应包含：

1. 目标理解
2. 阶段拆解
3. 每个阶段下的任务节点
4. 每个节点的执行路径
5. 推荐工具
6. 推荐资源链接
7. 完成标准
8. 可沉淀能力标签

### 5.3 推荐输出结构

```json
{
  "goalTitle": "做一个AI网站",
  "goalSummary": "从产品构思到上线部署，完成一个可展示的AI网站项目。",
  "stages": [
    {
      "title": "明确产品方向",
      "description": "确定网站要解决的问题和目标用户。",
      "tasks": [
        {
          "title": "确定网站主题",
          "description": "选择一个适合新手完成的AI网站方向。",
          "path": [
            "列出3个感兴趣的主题",
            "选择一个最容易实现的方向",
            "写出目标用户和核心功能"
          ],
          "tools": [
            {
              "name": "ChatGPT",
              "usage": "辅助头脑风暴产品方向",
              "url": "https://chat.openai.com"
            }
          ],
          "resources": [
            {
              "title": "React 官方文档",
              "url": "https://react.dev"
            }
          ],
          "completionCriteria": [
            "确定一个网站主题",
            "写出目标用户",
            "列出3个核心功能"
          ],
          "skillTags": [
            "产品构思",
            "需求分析",
            "AI项目规划"
          ]
        }
      ]
    }
  ]
}
```

---

## 6. 核心功能四：任务节点展示

### 6.1 功能目标

将 AI 生成的任务路径展示给用户，让用户能够清晰知道下一步该做什么。

### 6.2 V1 展示方式

V1 不需要复杂树形动画，可以先采用轻量结构：

- 阶段卡片
- 任务节点列表
- 任务详情展开面板
- 任务完成状态
- 打卡按钮

### 6.3 每个任务节点展示内容

每个任务节点应包含：

- 任务名称
- 任务说明
- 执行路径
- 推荐工具
- 推荐资源链接
- 完成标准
- 预计可获得技能标签
- 打卡完成按钮

### 6.4 任务状态

V1 可以先使用最简单的状态：

```text
未完成 / 已完成
```

后续版本可扩展为：

```text
未开始 / 进行中 / 已完成 / 已跳过
```

---

## 7. 核心功能五：任务打卡完成

### 7.1 功能目标

用户完成一个任务节点后，可以通过打卡完成动作触发技能标签生成。

### 7.2 打卡字段

打卡时用户可以填写：

- 我完成了什么？
- 我产出了什么？
- 遇到了什么问题？

V1 中这些字段可以设置为可选，不强制填写，避免增加用户负担。

### 7.3 打卡请求示例

```json
{
  "userProfile": {},
  "goal": "做一个AI网站",
  "completedTask": {
    "title": "确定网站主题",
    "description": "选择一个适合新手完成的AI网站方向。",
    "completionNote": "我确定了做一个AI学习导航网站。"
  }
}
```

---

## 8. 核心功能六：技能标签生成与回写

### 8.1 功能目标

用户完成任务后，系统调用大模型分析该任务对应的核心能力，并生成新的技能标签，回写到用户标签体系中。

### 8.2 模型输出示例

```json
{
  "newSkillTags": [
    {
      "name": "产品构思",
      "level": "入门",
      "source": "确定网站主题",
      "reason": "用户完成了网站方向选择、目标用户定义和核心功能梳理。"
    },
    {
      "name": "需求分析",
      "level": "入门",
      "source": "确定网站主题",
      "reason": "用户能够将模糊目标转化为具体功能。"
    }
  ]
}
```

### 8.3 系统行为

打卡完成后，系统需要：

- 将新技能标签加入用户标签库。
- 如果已有相同标签，则增加熟练度或完成次数。
- 在用户成长档案中记录标签来源任务。
- 在任务节点上标记已完成。
- 将技能标签展示在用户个人画像中。

---

## 9. MCP / Skill 设计建议

V1 可以将 AI 能力拆成两个 Skill 或内部 Agent 能力。

### 9.1 任务拆解 Skill

输入：

```text
用户标签 + 15字任务目标
```

输出：

```text
阶段、任务节点、执行路径、工具、资源、完成标准、预测技能标签
```

职责：

- 理解用户目标。
- 结合用户标签控制拆解粒度。
- 输出可执行任务路径。
- 推荐工具和资源链接。
- 预测每个任务可能沉淀的能力标签。

### 9.2 能力标签提炼 Skill

输入：

```text
用户标签 + 当前目标 + 已完成任务 + 用户打卡内容
```

输出：

```text
新增技能标签、标签等级、原因、证据
```

职责：

- 分析用户完成的任务。
- 提炼核心能力。
- 生成技能标签。
- 为标签提供来源和原因。
- 支持用户画像持续丰富。

---

## 10. V1 页面结构

第一版建议只做 5 个核心页面：

1. 用户标签选择页
2. 目标输入页
3. AI 生成中页面
4. 任务路径页
5. 我的技能标签页

其中，任务详情可以先做成任务路径页内的展开面板，不必单独成页。

---

## 11. V1 数据模型建议

### 11.1 UserProfile

```ts
interface UserProfile {
  id: string
  identityTags: string[]
  backgroundTags: string[]
  levelTags: string[]
  goalPreferenceTags: string[]
  skillTags: SkillTag[]
}
```

### 11.2 Goal

```ts
interface Goal {
  id: string
  userId: string
  title: string
  rawInput: string
  profileSnapshot: UserProfile
  status: "active" | "completed"
  createdAt: string
}
```

### 11.3 Stage

```ts
interface Stage {
  id: string
  goalId: string
  title: string
  description: string
  order: number
}
```

### 11.4 TaskNode

```ts
interface TaskNode {
  id: string
  goalId: string
  stageId: string
  title: string
  description: string
  pathSteps: string[]
  tools: ToolRecommendation[]
  resources: ResourceRecommendation[]
  completionCriteria: string[]
  predictedSkillTags: string[]
  status: "pending" | "completed"
}
```

### 11.5 SkillTag

```ts
interface SkillTag {
  id: string
  name: string
  level: "入门" | "进阶" | "熟练"
  sourceTaskId: string
  sourceGoalId: string
  evidence: string
  count: number
  createdAt: string
  updatedAt: string
}
```

### 11.6 ToolRecommendation

```ts
interface ToolRecommendation {
  name: string
  usage: string
  url: string
}
```

### 11.7 ResourceRecommendation

```ts
interface ResourceRecommendation {
  title: string
  description?: string
  url: string
}
```

---

## 12. V1 不做范围

为了保证第一版快速落地，以下能力暂不进入 V1：

- 不做复杂社区
- 不做用户之间分享任务树
- 不做自动调用外部工具执行任务
- 不做复杂成长树动画
- 不做官方内容认证后台
- 不做完整课程系统
- 不做多角色团队协作
- 不做真实能力考试认证
- 不做 Agent 市场
- 不做付费内容体系

这些能力可以作为 V2 或后续版本规划。

---

## 13. V1 验收标准

V1 成功的标准是用户能完成以下完整路径：

```text
选择标签
输入短目标
生成任务拆解
查看每个任务的路径、工具和资源
完成至少一个任务打卡
系统生成新的技能标签
技能标签出现在用户画像中
```

对应验收项：

- 用户可以完成标签选择。
- 用户可以输入 15 字以内任务目标。
- 系统可以基于标签和目标生成任务路径。
- 每个任务节点包含路径、工具、资源、完成标准和预测技能标签。
- 用户可以对任务节点打卡完成。
- 打卡后系统可以生成新的技能标签。
- 新技能标签可以回写到用户画像。
- 用户可以在“我的技能标签”页面看到新增标签。

---

## 14. V1 核心指标

建议第一版重点观察以下指标：

- 标签选择完成率
- 目标输入完成率
- AI 任务生成成功率
- 任务节点点击率
- 首个任务打卡率
- 技能标签生成成功率
- 用户是否愿意继续完成下一个任务

最关键的验证指标是：

```text
首个任务打卡率 + 技能标签回写成功率
```

因为这两个指标决定 LeavesFlow 是否真正形成了成长闭环。

---

## 15. V1 一句话总结

LeavesFlow V1 是一个通过标签封装 Prompt，让用户输入短目标即可生成 AI 任务路径，并在任务打卡后自动沉淀技能标签的成长导航系统。